var user;
var sequre;
do{
    user=parseInt(prompt("Enter numbers"));
if(isNaN(user))
{
    alert("should enter number");
    continue;
}

    
     squre=Math.sqrt(user);

}while(!user )
alert("the squre of number :"+user +" is "+ +squre);
console.log("the squre of number :"+user +" is "+ +squre);
